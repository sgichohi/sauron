Installing 

pip install -r requirements.txt

How to run:

python camera.py c++recepientport webserverport

Testing:

open 127.0.0.1:webserverport/do/identity

Take the /static/*/* part from the List of URLS and chunk it into a link as so.

127.0.0.1:webserverport/earlier_static_url

You should see a picture.

I'll be plugging in the recipient from C++ when I work sth out with legendary Jonathan.

Design Docs:

To be written